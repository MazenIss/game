import { BetterhoverDirective } from './betterhover.directive';

describe('BetterhoverDirective', () => {
  it('should create an instance', () => {
    const directive = new BetterhoverDirective();
    expect(directive).toBeTruthy();
  });
});
